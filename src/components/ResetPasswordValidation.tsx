import React, { FC } from 'react';

const ResetPasswordValidation: FC = ({}) => {
  

  return (
    <div className='flex items-center flex-col h-2/3 w-full '>
     
    </div>
  );
};

export default ResetPasswordValidation;